1，锁屏界面必须要有时间，日期这两个参数
排列为
时间
日期
日期下方需要有一个动态显示区域（可能是用来放广告的）为了不显突兀，可以用通知的形式展示，例如微信通知气泡
应该可以这样做
textview
textview
scrollview，这样通知可以发送出来

功能要求，点击软件主界面的启动程序，等用户下次点亮屏幕的时候，需要弹出这个界面，
难点：是否需要关心用户的锁屏密码问题？悬浮窗权限能不能在锁屏界面起效？